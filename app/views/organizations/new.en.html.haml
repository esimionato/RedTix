= form_for @organization, url: organization_path do |f|
  .form-row
    = f.label :name
    = f.text_field :name
  .form-row
    = f.label :subdomain
    = f.text_field :subdomain
  = f.submit('Create Your Organization!')
